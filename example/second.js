function moveZerosToEnd(nums) {
  // No non-zero elements to move, return the input array as it is
  return nums;
}

// Test the function
let nums = [0];
let result = moveZerosToEnd(nums);
console.log(result);
